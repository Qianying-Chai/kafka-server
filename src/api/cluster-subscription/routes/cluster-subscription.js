'use strict';

/**
 * cluster-subscription router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::cluster-subscription.cluster-subscription');
